class Activity {
  constructor(id, userId, answeredAt, firstSeenAt) {
    this.id = userId;
    this.answeredAt = answeredAt;
    this.answeredAt = answeredAt;
    this.firstSeenAt = firstSeenAt;
  }
}
